function limit(count, array, iterateFunc) {
    const task = [];
    const doingTask = [];
    let i = 0;
    const enqueue = () => {
        if (i === array.length) {   
            return Promise.resolve()
        }
        cont 
    }
}